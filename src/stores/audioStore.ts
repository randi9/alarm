// Audio store is handled by the useAudioPlayer composable
// This file is reserved for future store needs
import { defineStore } from 'pinia'

export const useAudioStore = defineStore('audio', {
  state: () => ({
    // Reserved for future use with backend integration
  })
})
